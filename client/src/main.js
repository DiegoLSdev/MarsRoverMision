import { createApp } from 'vue'
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')

// make a call into the backend endpoint start and, store the result and render it in the app
